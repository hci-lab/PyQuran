from setuptools import setup, find_packages

setup(
   name='PyQuran',
   version='1.0.0',
   description='low-level analysis on Quran',
   url='https://github.com/TahaMagdy/PyQuran',
   author='TheTeam',
   author_email='',
   packages=find_packages(),
   install_requires=[]
)

# After this run
# $ sudo pip3 install -e myPackage
